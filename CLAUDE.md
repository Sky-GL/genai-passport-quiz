# 生成AIパスポート試験対策アプリ - 開発規約

## プロジェクト概要
生成AIパスポート試験の対策用クイズアプリ。Expo (React Native) 製、Android APKとしてEAS Buildで配布。
このリポジトリには本アプリ（ルート）と、別プロジェクトの`toeic990/`（Next.js製TOEIC対策アプリ）が同居している。作業対象がどちらか都度確認する。

## 技術スタック
- フレームワーク: Expo (`~52.0.0`) + expo-router + React Native (`0.76.3`) + TypeScript
- ビルド/配布: EAS Build（`eas build -p android --profile preview`）でAndroid APKを生成
- 問題データ・API呼び出し: `app/quiz-data.js`（現在はオフライン版へ移行済み。Anthropic APIキーは未使用）

## セキュリティ上の注意
- `config.js`のAPIキーは`EXPO_PUBLIC_`プレフィックスの環境変数から読む設計だが、この接頭辞の変数はクライアントバンドルに埋め込まれ端末側から抽出され得る。配布用ビルドで外部APIキーを使う場合はサーバー経由の呼び出しに移行すること
- APIキーやシークレットをコード・コミットに直接書き込まない

## ユーザー向け説明ルール（重要）
このプロジェクトのユーザーはエンジニアではない。Windows環境でのコマンド操作やExpo/EASのビルド手順に不慣れなため、作業を依頼・説明する際は以下を徹底する。

- **コマンドは常にコピペで実行できる完全な形で渡す**
- **UI操作は具体的な画面名・ボタン名で説明する**（例: 「設定を開く」ではなく「Androidの設定→セキュリティ→提供元不明のアプリ」）。expo-router、EAS、環境変数などの専門用語は初出時に一言補足する
- **一度に大量の手順を出さない**。1〜2ステップごとに確認を挟む
- **エラー時はまずスクリーンショットをお願いする**
- ビルドやストアの画面は時期によって変わるため、断定しすぎず「〜のはずです」「見当たらなければスクリーンショットを」という前提で案内する

## ループエンジニアリング（自律開発ワークフロー・デフォルト方針）
**今後このプロジェクトでコードを作成・修正する際は、規模に関わらず原則としてループエンジニアリングを適用する。** 従来の「プロンプト→開発→確認→修正」を人間が都度手動で回すやり方に対し、完了判断だけを人間が担い、それ以外の反復作業（実装→検証→修正）はAIエージェント側で完結させる。

- **レベル1: 単一ゴールによる自動検証（基本形・常時適用）** — 1機能=1ゴールとして、着手前に仕様と検証チェックリスト（完了条件）を明確化し、チェックリストを満たすまで自律的に実装・確認・修正を繰り返す。小さな修正でも「何を満たせば完了か」を先に言語化してから着手する
- **レベル2: 複数機能のキュー処理** — 複数の機能追加/修正をまとめて依頼された場合はタスクキューとして積み、「開発担当」と「敵対的レビュー担当（バグを探す役）」の視点を分けて品質を担保しながら連続処理する
- **レベル3: 非同期での計画承認・最終判断** — 実装そのものはエージェントに任せ、人間はマイルストーンごとの計画承認とリリース可否の最終判断のみ行う

適用ルール:
- **着手前に完了条件を明示する**。曖昧な依頼でも、実装前に検証チェックリスト（何をもって完了とするか）を組み立ててから進める
- **完了判断は必ず人間に委ねる**。チェックリストを満たしたと判断したら、その根拠（動作確認内容、ビルド結果など）を添えて報告し、最終OKを待つ
- 「ユーザー向け説明ルール」（本ファイル上部）は自律開発でも変わらない。反復作業がバックグラウンドで進んでも、ユーザーへの報告・確認は1〜2ステップごとに区切って行う
- EASビルド・ストア申請など手動操作やクラウド上の課金が発生する作業は、自律ループの外に出し、人間の確認ポイントとして明示する

## コーディング規約
- コメントは日本語、自明な処理には付けない
- 動作優先、リファクタは指示があった時のみ
